<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Apptopia"
        tools:targetApi="31">
        <activity
            android:name=".login_page"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".firstfloor_discussion_room_activity"/>
        <activity android:name=".main_page"/>
        <activity android:name=".fragment_change"/>
        <activity android:name=".notifi_board"/>
        <activity android:name=".freewrite_board"/>
        <activity android:name=".library_guide"/>
        <activity android:name=".writing_board"/>
        <activity android:name=".notebook_zoon"/>
        <activity android:name=".discussion_room"/>
        <activity android:name=".reading_room"/>
    </application>

</manifest>